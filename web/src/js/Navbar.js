var React = require('react');

var Navbar = React.createClass({
  render: function() {
    return (
      <nav className="navbar">
        <h1>
          YAMLCal
        </h1>
      </nav>
    );
  }
});

module.exports = Navbar;
